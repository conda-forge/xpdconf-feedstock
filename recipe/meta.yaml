{% set name = "xpdConf" %}
{% set version = "0.4.2" %}
{% set sha256 = "a634d766eda524e3110c78a40de251e0420253394eab456aa843210f124710e8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/xpdAcq/{{ name }}/releases/download/{{ version }}/xpdConf-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - databroker
    - pyyaml

test:
  imports:
    - xpdconf

about:
  home: http://github.com/xpdAcq/xpdConf
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Configuration for XPD beamlines

extra:
  recipe-maintainers:
    - chiahaoliu
    - sbillinge
